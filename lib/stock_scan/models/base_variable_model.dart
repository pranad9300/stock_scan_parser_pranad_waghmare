// Define the abstract VariableModel class with specified properties and types
abstract class VariableModel {
  // The type of the variable
  String variableType;

  // Constructor for initializing VariableModel instances
  VariableModel({
    required this.variableType,
  });
}
